'use client';

import { CalculatorPage } from '@/entities/calculator/CalculatorPage';

export default function PageClient() {
  return (
    <section>
      <div className="container ">
        <CalculatorPage />
      </div>
    </section>
  );
}
